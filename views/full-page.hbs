 <div class="course">
    <h1>{{course.title}}</h1>

    <div class="img-wrapper">
        <img src={{course.image}} />
    </div>

    <span class="price">{{course.price}}</span>

 </div>
            